# 🌙 ختمة رمضان الجماعية

تطبيق ويب لتنظيم ختمة القرآن الكريم جماعياً في رمضان، حيث يختار كل شخص جزءاً مختلفاً بدون تكرار.

---

## ✨ المميزات

- 🌙 تصميم رمضاني بالكامل (نجوم، هلال، فانوس)
- 👤 كل مشارك يختار اسمه + أفاتار بأسماء الصحابة
- 📖 حجز الأجزاء الثلاثين بدون تكرار
- ⚡ **حجز ذري (Atomic)** عبر Firebase Transactions — مستحيل يحجز اتنين نفس الجزء
- 🔄 تحديث فوري لجميع المتصلين في نفس الوقت (Real-time)
- 📊 لوحة متابعة مع شريط تقدم الختمة

---

## 🚀 خطوات التشغيل

### الخطوة 1 — إنشاء مشروع Firebase

1. افتح [console.firebase.google.com](https://console.firebase.google.com)
2. اضغط **"Add project"** → أدخل اسم المشروع
3. افتح **Realtime Database** من القائمة الجانبية
4. اضغط **"Create Database"** → اختر موقع قريب → ابدأ بـ **test mode**
5. من إعدادات المشروع ← **Project settings** ← **Your apps** ← أضف **Web app**
6. انسخ بيانات `firebaseConfig`

### الخطوة 2 — إعداد ملف البيئة

```bash
cp .env.example .env
```

افتح `.env` وضع بياناتك:

```env
VITE_FIREBASE_API_KEY=AIza...
VITE_FIREBASE_AUTH_DOMAIN=my-project.firebaseapp.com
VITE_FIREBASE_DATABASE_URL=https://my-project-default-rtdb.firebaseio.com
VITE_FIREBASE_PROJECT_ID=my-project
VITE_FIREBASE_STORAGE_BUCKET=my-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abc123
```

### الخطوة 3 — تثبيت وتشغيل

```bash
npm install
npm run dev
```

افتح المتصفح على `http://localhost:5173`

---

## 📦 النشر على Vercel (مجاناً)

```bash
npm install -g vercel
vercel
```

ثم أضف متغيرات البيئة في Vercel Dashboard تحت **Settings → Environment Variables**

---

## 📦 النشر على GitHub Pages

```bash
npm run build
```

ارفع محتوى مجلد `dist` على GitHub Pages.

---

## 🔒 قواعد Firebase Realtime Database

بعد ما تنتهي من الاختبار، غيّر القواعد لتكون أكثر أماناً:

```json
{
  "rules": {
    "participants": {
      ".read": true,
      "part_$partNumber": {
        ".write": "!data.exists()"
      }
    }
  }
}
```

هذه القواعد تسمح بالقراءة للجميع، لكن **لا يمكن تعديل جزء محجوز مرة ثانية**.

---

## 🛠️ التقنيات المستخدمة

| التقنية | الاستخدام |
|---------|-----------|
| React 18 | واجهة المستخدم |
| Vite | بناء المشروع |
| Firebase Realtime DB | تخزين مشترك + Real-time |
| Google Fonts (Amiri, Tajawal) | الخطوط العربية |

---

## 📁 هيكل المشروع

```
khatma/
├── src/
│   ├── main.jsx      ← نقطة الدخول
│   ├── App.jsx       ← التطبيق كاملاً
│   └── firebase.js   ← إعداد Firebase
├── index.html
├── vite.config.js
├── .env.example      ← نموذج متغيرات البيئة
└── package.json
```

---

رمضان كريم 🌙
